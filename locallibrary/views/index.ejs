<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="stylesheet" href='https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css'>
  </head>
  <body>
    <h1><%= title %></h1>
    
    <p>Welcome to LocalLibrary, a very basic Express website developed as a tutorial example on the Mozilla Developer Network.</p>

    <nav>
      <%- include('./layout'); %>
    </nav>

    <% if (error) { %>
      <p>Error getting dynamic content.</p>
    <% } else { %>
      <p>The library has the following record counts: </p>
      <ul>
        <li>
          Books: <%= data.book_count %>
        </li>
        <li>
          Copies: <%= data.book_instance_count %>
        </li>
        <li>
          Copies available:  <%= data.book_instance_available_count %>
        </li>
        <li>
          Authors:  <%= data.author_count %>
        </li>
        <li>
          Genres: <%= data.genre_count %>
        </li>
      </ul>

    <% } %>

  </body>
</html>
